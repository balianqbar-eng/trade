# 開啟戰情儀表板前置清單

> 標示 🤖 的步驟可由 Claude 自動執行，只需說「幫我跑路線一/二」

## 路線一：使用永豐

- 🤖 確認 `.env` 有 `SINOPAC_API_KEY`、`SINOPAC_SECRET_KEY`
- 🤖 啟動 server：
  ```bash
  cd /Users/balianwang/Downloads/balian
  python stock_server.py
  ```
- 🤖 登入永豐：
  ```bash
  curl -X POST http://localhost:8000/login
  ```
- [ ] 手動：開瀏覽器 `http://localhost:8000`，右上角選「永豐」

---

## 路線二：使用 XQ

- [ ] 手動（Win11）：開啟 XQ 軟體
- [ ] 手動（Win11）：執行 `python xq_bridge/server.py`（port 8000）
- [ ] 手動（Win11）：啟動 ngrok tunnel：
  ```
  ngrok http 8000
  ```
- 🤖 更新 Mac `.env` 的 ngrok 網址（貼給 Claude 後自動寫入）
- 🤖 啟動 Mac server：
  ```bash
  python stock_server.py
  ```
- [ ] 手動：開瀏覽器 `http://localhost:8000`，右上角選「XQ」
