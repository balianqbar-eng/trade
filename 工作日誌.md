# 工作日誌

---

## 2026-05-15

### 完成項目

**1. Obsidian 架構筆記整理**
- 將 `dashboard_architecture.html` 拆解為 6 個原子筆記，放入 `02 案例庫/戰情儀表板/`
- 建立 MOC 入口 + 系統總覽 / 分層架構 / 功能Tabs / V2實驗Tab / API端點地圖 / 問題診斷
- 原始 HTML 備份至 `00 原始資料/`，節點間用 `[[wiki-link]]` 串接

**2. 健檢評分 PE/殖利率即時化**
- 後端 `/fundamental/{ticker}` 新增 `twseClose`（TWSE 昨收）、`annualDivPerShare`（年化股利元）
- 前端 `loadV2Health()` 並行抓 `/quote/{ticker}`，有即時價時用 `即時價/eps4q` 重算 PE，`年化股利/即時價` 重算殖利率
- 即時重算的數值後加 `⚡` 標記，永豐未登入時靜默 fallback 回 TWSE 昨收值

**3. 大單買賣棒狀圖**
- 後端 `/bigvol/{ticker}` 改版：tick 按 `tick_type`（1=外盤買/2=內盤賣）分類，按分鐘聚合成 `bars[]`，計算 `net_lots` + `signal`（buy/sell/neutral，門檻為總量 20%）
- 前端 K 線 tab 即時模式新增：淨買超數字（紅/綠）、訊號標籤（大單淨買超/賣超）、ECharts 雙色堆疊棒狀圖（買紅上/賣綠下）
- 有永豐登入才顯示，未登入靜默

### 問題與解法
- PE/殖利率只有盤中才即時，盤後 `/quote` 回 503 → fallback 回 TWSE 昨收，設計上無需修改
- 大單棒狀圖 `tick_type` 依賴永豐登入，收盤或未登入時顯示 `—`，符合預期

### 待處理
- XQ COM 需互動式 session 問題尚未解決（需手動在 Parallels 開啟 XQ）
- DMR #5（遠端工作環境通電）待 Windows 工作流規劃完成後確認

### 備註
- `failures.md` 有異動（前次 session 累積）

---

## 2026-05-14（DMR 工作記錄機制建立）

### 完成項目

**DMR 代碼新增進習慣動作代碼表**
- `feedback_jilu.md` 新增 DMR（Daily Mission Record）條目
- 觸發時機：每結束一件工作時 → 寫一筆紀錄
- 欄位：`row_id, start_time, end_time, duration_min, project, summary, progress, status, needs_confirm, notes`
- 提醒機制：新對話開場跑 `dmr.py list-pending`，主動追使用者「待確認」項目

**dmr.py CLI 工具**
- 位置 `/Users/balianwang/Downloads/balian/dmr/dmr.py`，複用 video2slides 的 venv（含 google-api-python-client）
- 子指令：`init` / `log` / `list-pending` / `mark-done`
- 雙寫策略：本地 CSV `/Users/balianwang/Downloads/balian/工作記錄.csv` + Google Sheet

**Google Sheet 雲端同步**
- 建立試算表「工作記錄 DMR」：https://docs.google.com/spreadsheets/d/1NytNAkKEcdJ1iYhJfFztXIRC5OZY0l2IxrcysGUJdZY/edit
- OAuth 擴充 scope（spreadsheets + drive.file），token 存 `dmr/token_dmr.json`，跟 video2slides token 隔離
- Sheet ID 存 `dmr/.dmr_config.json`

### 問題與解法
- 第一次 `dmr.py init` 卡住沒輸出 → kill 重跑，OAuth 完成後失敗顯示真正原因
- Sheets API 在 GCP project 447283172552 未啟用 → 引導使用者去 console 按 Enable 後重試成功
- OAuth `run_local_server` 沒自動彈瀏覽器（推測 Cmux/Terminal 環境）→ 使用者貼 console URL 完成授權，token 正常產出

### 待處理
- 寫第一筆 DMR 試水溫（建議用「DMR 機制建立」本身當第一筆）
- 觀察一週累積資料後評估 Sheet 欄位是否要調整

### 備註
- DMR 呼叫範例：
  ```
  /Users/balianwang/Downloads/balian/video2slides/.venv/bin/python \
    /Users/balianwang/Downloads/balian/dmr/dmr.py log \
    --start "..." --end "..." --duration N --project ... \
    --summary "..." --status completed --needs-confirm N
  ```
- Claude 新對話開場必跑 `dmr.py list-pending`（已寫入 memory）

---

## 2026-05-14（Claude Code 貼上折疊查驗 + Ratchet R002）

### 完成項目

**Claude Code 貼上折疊問題調查**
- 翻完 settings.json 完整 schema + Claude Code 2.1.140 binary 的所有環境變數
- 結論：**沒有任何設定可以關掉 `[Pasted text #N +M lines]` 折疊行為**，是 hardcoded TUI 邏輯
- 替代方向已盤點：接受折疊 / 存檔 `@檔名` 引用 / 關 terminal bracketed paste / 開 issue

**YouTube 影片字幕交叉比對**
- 用 yt-dlp 抓 `xo7dE80ktu4` 的 zh-TW 字幕完整翻過
- 確認影片沒教這個解法；最接近的章節 15:27 CMUX 解的是 Shift+Enter 直接送出問題（使用者已在用 Cmux）

**CotEditor 設為外部編輯器（部分有效，但不解原問題）**
- `~/.zshrc` EDITOR 從壞值 `cot-editor` 修為 `/Applications/CotEditor.app/Contents/SharedSupport/bin/cot -w`
- 觸發 macOS Automation 權限授權（cmux → CotEditor 打勾）
- 驗證 cot CLI 在新 shell 可呼叫
- 但 — **這條只影響 `/memory`、Plan mode、`externalEditorContext` toggle**，不影響當前輸入欄草稿

**Ratchet R002 寫入 `failures.md`**
- 失敗訊號：拿 binary `strings` 撈到的 hint 字串就斷言「double-ESC 開外部編輯器」，未驗證就叫使用者改 zshrc / 開系統權限 / 重啟
- 實際 double-ESC 行為：清空輸入欄 或 倒回上一則對話
- Harness 層：觀測層（單一字串證據過度外推）+ 約束層（沒擋「未驗證不准建議改系統狀態」）
- 永久規則三條（詳見 failures.md R002）

### 問題與解法
- yt-dlp 在 pyenv 環境下要走 `~/.pyenv/versions/3.11.9/bin/yt-dlp`，pip install 後 shim 沒接上
- cot CLI 第一次跑失敗 — AppleEvent timeout (-1712)：cmux 沒有「控制 CotEditor」的 Automation 權限 → 在系統設定面板手動授權
- 我自己的失敗：用片段 binary 字串推論功能行為 → 已 ratchet 進 R002

### 待處理
- 真正解決貼上折疊：考慮去 anthropics/claude-code 開 feature request

### 備註
- Cmux 是當前 terminal wrapper（env `CMUX_CLAUDE_PID` 可確認）
- Claude Code 真正會用 $EDITOR 的三個地方：`/memory`、Plan mode 編輯、`externalEditorContext` toggle 顯示最後一則回覆

---

## 2026-05-14（戰情儀表板策略回測功能）

### 完成項目

**回測功能（K線 tab）**

- `stock_server.py`：新增 `GET /qianshao/backtest?ticker=&strategy_id=&hold_days=5` endpoint
  - 抓 TWSE 90 根日K
  - 從第 26 根起逐根計算技術指標（`calc_technical`），評估策略的技術面條件
  - 條件全中 → 記錄進場，持有 N 日後出場，計算報酬
  - 回傳：kbars + signal_indices + trades 明細 + stats（次數/勝率/平均報酬）
- `dashboard.html`：K線 tab 加「即時 / 回測」模式切換
  - 回測模式：策略下拉、持有天數輸入、執行按鈕
  - 引入 ECharts 5，日K 蠟燭圖 + 進場點綠三角 markPoint
  - 統計卡：訊號次數、獲利次數、勝率、平均報酬
  - 交易明細表（進出日期、價格、報酬%）

### 限制 / 已知缺口
- 只支援技術面條件回測（籌碼/基本面條件因歷史資料不可得會跳過）
- TWSE API 約提供近 90 根日K（3 個月）

### smoke test
- 11 通過，0 失敗

---

## 2026-05-14（PDCA 平台規劃 + R001 ratchet + 目標.md v0.1）

### 完成項目

**PDCA 專案開發分析平台規劃對話**
- 使用者初始需求：「能看到今天做了什麼、何時做、花多少時間、與目標相關性」
- 教練擋下：P（Plan）完全沒寫，PDCA 失去基準
- 結構化拷問 4 題收斂 done-condition：每日報告 + 週儀表板 + 即時查詢 + PDCA 提醒（使用者全選）
- 工時帳：4 模組合計 ~6 個月工程量，3 個月做不完
- 收斂結果：使用者選版本 A 精簡 V1（主目標）；PDCA 平台本身延後到 M1 結束（2026-06-14）重評估

**R001 Ratchet：新專案 kickoff 前必須有可驗證目標檔案**
- 失敗訊號：使用者要做 PDCA 平台但目標完全在腦袋裡，沒寫下來
- Harness 層：規則層（缺強制 kickoff 前要有目標檔案的擋線）
- 永久規則：教練看到「規劃 / 建立 / 做一套 XX」要先檢查 `目標.md` / `spec.md`，沒有就先 Phase 0 目標出土；「完整版」「全功能」「做好」屬空話必須拆
- 寫入 `failures.md` R001（已 commit）

**目標.md v0.1 草稿建立**
- 位置：`/Users/balianwang/Downloads/balian/目標.md`
- 主目標：股票戰情室 V1（精簡版，2026-05-14 → 2026-08-14，13 週）
- 子目標 1：半自動下單系統（M1）— Telegram 推播 + 確認 + 永豐 API
- 子目標 2：回測引擎 V1（M2）— 核心指標 + 對標 0050
- 子目標 3：檢視儀表板 V1（M3）— 訊號分頁 + 持倉分頁
- 不在 V1：全自動下單 / 視覺化圖表 / 多策略並列 / 風控自動化（全列 V2）
- 其他專案：維護模式
- 延後重評估清單：PDCA 平台 → 2026-06-14

### 問題與解法
- 「目標只在腦袋裡」這個 anti-pattern 之前沒對應 ratchet → 補成 R001

### 待處理（阻塞 v1.0 lock）
- 校稿訊號跑通數（草稿 10）
- 校稿回測策略清單（草稿 3 個含 3 分 K 當沖）
- 校稿回測期間（草稿 2024-01-01 → 2025-12-31）

### smoke test
- 11 通過，0 失敗

---

## 2026-05-13（網頁部署分享服務）

### 完成項目

**Benjamin Dashboard：網頁部署分享用**
- `balian-quant/main.py` 新增 `/share` 頁面 + `/api/deploy/share` endpoint
- 新建 `share.html`：拖拉/點選/⌘V 貼上檔案、HTML 原始碼貼 textarea、最近 10 筆 localStorage 歷史、Finder ⌘C → ⌘V 自動偵測檔案
- UI：漸層大標、滑鼠雷達 hover、卡片化版面、響應式 grid 歷史紀錄、`file://` 與絕對路徑誤貼警示
- `projects.json` 加入 `share` 專案 entry（指向 `/share`）
- `requirements.txt`：`python-multipart`、`blake3`

**部署平台演進**
1. Netlify Drop Playwright 拖檔上傳（單檔可行但只是 PoC）
2. Netlify API 直傳 zip → Content-Type 變 `text/plain`（瀏覽器當純文字顯示原始碼）
3. 改 Netlify file digest 流程（sha1 manifest + PUT files）→ Content-Type 正常 `text/html`
4. 連續測試燒光 Netlify 月度 credit（403 Account credit usage exceeded）
5. 註冊 Cloudflare 帳號（Google 一鍵登入 balianqbar）→ 內部 API `/api/v4/user/tokens` 直接建 Pages Write token（`8d28297797f24fb8a0c332fe0866ec89` permission group），繞過 react-select UI
6. 建立 Cloudflare Pages project `balian-share`；Python 實作 Direct Upload：取 upload-jwt → blake3 hash check-missing → base64 upload → multipart manifest deployment
7. 實測通過，preview URL 形式 `https://<hash>.balian-share.pages.dev`，Content-Type 正常

### 問題與解法
- **Netlify zip 直傳 Content-Type 錯誤**：改 file digest（讓 Netlify 依路徑判 mime）
- **Netlify credit 用光**：改 Cloudflare Pages
- **react-select dropdown 無法用 Playwright 觸發**：改打 Cloudflare 內部 API 建 token
- **使用者誤把 `file://` 路徑貼 HTML 欄**：textarea 偵測 `file://` / 絕對路徑，提示改用拖拉或 Finder ⌘C → ⌘V
- **中文 name 經 regex slug 變 `-------`**：直接編 `projects.json` 給乾淨 id `share`

### Railway 環境變數
- `NETLIFY_AUTH_TOKEN`（保留）
- `CLOUDFLARE_ACCOUNT_ID` / `CLOUDFLARE_API_TOKEN` / `CLOUDFLARE_PAGES_PROJECT=balian-share`

### 待處理
- 舊測試 Netlify sites 清理（enchanting-babka、splendorous-crostata 等）

### 備註
- Production URL：`https://balian-share.pages.dev`（永遠最新）
- 每次部署的 preview URL：`https://<hash>.balian-share.pages.dev`

---

## 2026-05-13（Obsidian 知識庫建立）

### 完成項目
- 安裝 Obsidian 1.12.7（Homebrew cask）
- 建立 AI時代卡位框架 Vault（`~/Documents/AI時代卡位框架/`）
  - 來源：`思維備忘錄_AI時代的價值與卡位.md`
  - 拆解為 15 個原子化筆記：核心概念 5、案例庫 4、待深挖議題 6、破點 5
  - 建立 MOC 主索引（`00 MOC`）
  - 原始資料備份至 `00 原始資料/`，加索引表格指向各子筆記
- Graph View 分組顏色設定（紫/綠/藍/橘/紅 對應五個資料夾）
- Obsidian 操作教學：wiki-link、反向連結、Graph View、Markdown 語法
- 建立工作流程：未來對話資料由 Claude 拆解整理進 Vault

### 備註
- Vault 路徑：`/Users/balianwang/Documents/AI時代卡位框架/`
- 今日無 git commit（Obsidian 資料夾不在 balian repo 內）
- smoke test：11 通過，0 失敗

---

## 2026-05-12（Claude Code 工具鏈升級 + Benjamin 教練模式）

### 完成項目

**agent-skills 整合（~/.claude 全域）**
- 從 addyosmani/agent-skills 安裝三個 specialist agents：`code-reviewer`、`security-auditor`、`test-engineer`
- 安裝 sdd-cache WebFetch 快取 hooks（`sdd-cache-pre.sh` / `sdd-cache-post.sh`）並註冊進 `~/.claude/settings.json`
- 新增 CR 代碼到代碼表（呼叫 code-reviewer agent）

**harness-coach 全域安裝**
- 安裝 Harness Engineering 教練到 `~/.claude/`（7 個 slash commands、2 個 skills、1 個 agent）
- 建立 `~/failures.md` 個人 ratchet 紀錄
- 教練規則 append 到 `~/.claude/CLAUDE.md`

**Benjamin 教練模式 toggle**
- `dashboard.html` 新增「教練模式 ON/OFF」按鈕（per-project，琥珀色高亮）
- 狀態存進 `projects.json` 的 `coach_mode` 欄位
- 部署到 Railway（balian-quant-production）

### 問題與解法
- balian-quant 本地跑不起來（需要 XQ 環境），只能 railway up 部署看結果

### 待處理
-

### 備註

---

## 2026-05-10（video2slides nschool.tw 支援 + 歷史紀錄 UI）

### 完成項目

**nschool.tw 課程影片自動下載**
- 新增 `src/nschool.py`：自動從 Chrome 讀取 nschool.tw Cookie（AES-128-CBC 解密，跳過 32 bytes hash prefix）
- POST `/api/v1/auth/refresh-token` 換取 JWT
- Hasura GraphQL 查詢 `program_content_by_pk`，取得 `attachment_id`
- GET `/api/v2/videos/{attachment_id}/sign` 取得 CloudFront 簽名 HLS URL
- m3u8 sub-playlist 重寫：將相對路徑 segment 改為附帶完整 CloudFront query string 的絕對 URL，再交 ffmpeg 下載
- `downloader.py` 加入 nschool.tw URL routing

**UI 近期下載歷史**
- `ui.html` 新增歷史紀錄卡，顯示近 10 筆完成工作
- 每筆顯示標題、相對時間、Slides / PPTX / PDF 連結
- 頁面載入和工作完成時自動更新

### 問題與解法
- Chrome cookie 解密：`meta_version=24` 表示 plaintext 前 32 bytes 是 hash prefix，需 `plaintext[32:]`
- GraphQL 欄位用 `attachment_id` 非 `id`（後者回傳 `E_ATTACHMENT`）
- ffmpeg 無法處理相對路徑 segment：手動重寫 m3u8 加入完整 signed URL

---

## 2026-05-10（video2slides 圖書館管理系統 + Dispatch 設定）

### 完成項目

**Dispatch 設定**
- 桌電 Claude Desktop 更新至最新版並開啟 Keep awake，手機 Dispatch 連線成功
- 整理從手機 Dispatch 派任務的 prompt 模板，可直接貼上執行 video2slides pipeline

**Notion 影音圖書館**
- 建立 Notion 資料庫「影音圖書館」（8 個欄位：標題/分類/主題標籤/原始網址/Google Slides/摘要/轉換日期/狀態）
- 新增首筆記錄：ETF 投資新手入門指南，含 Google Slides 可點連結
- 設定一週後（2026-05-17）使用心得問卷提醒

**Google Drive 影音圖書館**
- 建立 Drive 資料夾「影音圖書館」（folder ID: 1AZ9aRWfwpvj6mRhLOQgSYJ_ZfU5MizkM）
- 修改 `src/slides_generator.py`：新增 `_move_to_library()`，每次產出後自動移入資料夾
- 將既有 ETF 新手入門指南簡報手動移入

**其他**
- 建立 `video2slides/CLAUDE.md`
- 新增 feedback memory：收到新問題先確認理解 + 優化 + 指出未考慮到的地方（回覆類訊息不觸發）

### Smoke test
11 通過，0 失敗

### Why
video2slides 從單純的轉換工具升級為有系統管理能力的知識圖書館，手機 Dispatch 讓操作更流暢，Notion + Drive 雙軌讓查找和儲存分開管理。

---

## 2026-05-10（video2slides 知識庫輸出機制）

### 完成項目（video2slides pipeline 擴充）

- 新增 `src/knowledge_exporter.py`：每次轉換後自動產出 agent 可用的 `knowledge.md`
- 知識文件結構：YAML frontmatter + 學習目標 / 前置知識 / 核心內容（含 speaker_notes）/ 重點提醒 / 練習題 / 完整逐字稿
- 固定分類清單：財經、心理學、生活、AI運用、其他；由 Gemini 自動判斷歸類
- 輸出至 `output/knowledge/{分類}/{標題}_knowledge.md`，並維護 `output/knowledge/index.json` 供 agent 查索引
- 整合至 `server.py`：`analyze()` 完成後自動觸發，不新增 pipeline 階段

### Smoke test
11 通過，0 失敗

### Why
video2slides 的目標是雙軌輸出：slides 供使用者學習，knowledge.md 供 agent 吸收提升專業深度，兩條線獨立不干擾。

---

## 2026-05-09（深夜 V2 實驗 tab 完整實作）

### 背景
參考使用者提供的「持股分析健檢系統」、「AI 看盤數據分析儀表板」、「可轉債盤中評分監控台」、「衍生性金融商品即時資訊監控台」與釣魚選股 V8 截圖，提出 6 個進階功能規劃並全部實作完成（前端 UI + 後端 API + 真實資料串接）。

### 完成項目（戰情儀表板 V2 實驗 tab）

**前端 (`dashboard.html`)**
- 新增「V2 實驗」tab（藍色虛線框 + NEW badge），位置在消息面之後
- 內部 sub-nav 4 子頁：健檢評分 / 籌碼雷達 / 大盤情緒 / 主力追蹤
- 上方常駐事件預警條（融券回補日 / 除權息 / 融券餘額）
- 引入 Chart.js 4 用於雷達圖與長條圖

**後端 (`stock_server.py`)**
- 新增 `/events/{ticker}` — TWSE t187ap45_L 抓除權息 + MI_MARGN 抓融券餘額；股東會日 - 6 營業日推算融券強制回補日
- 新增 `/macro/dashboard` — TAIFEX OpenAPI（PCR、期貨日報、選擇權日報）+ TWSE MI_INDEX 加權指數；計算基差、Skew (Put-Call/Call %)、Max OI 履約價（限 ATM ±10%）、ATM 權利金；規則引擎產 AI 白話文判讀
- 新增 `/branch/{ticker}` — FinMind 三大法人歷史替代分點進出（FinMind 免費版分點需付費），附外資持股比、20 日累計排行
- 擴充 `/fundamental` — 加 EPS（近4季加總）、營收 YoY、淨利率、營業利益率（FinMind 財報 + 月營收）
- 擴充 `/chip?history_days=N` — 串 FinMind 三大法人歷史，回傳連續買賣超天數（正=連買、負=連賣）
- 新增 FinMind helper `_finmind()` 與 10 分鐘記憶體快取
- 新增常數 `FINMIND` / `TAIFEX` 端點 base URL

**功能對應**
| # | 功能 | 後端端點 | 資料源 |
|---|------|---------|--------|
| 1 | 健檢評分（4 維度 + Gauge + 6 健檢項目 + PR） | `/fundamental` 擴充 | TWSE BWIBBU_d + FinMind 財報/月營收 |
| 2 | 籌碼雷達（5 軸） + 連續買賣超 | `/chip?history_days=20` | TWSE T86 + FinMind 法人歷史 |
| 3 | 事件預警條 | `/events/{ticker}` | TWSE t187ap45_L + MI_MARGN |
| 4 | 大盤情緒（PCR/基差/OI/支撐壓力/決策矩陣） | `/macro/dashboard` | TAIFEX + TWSE MI_INDEX |
| 5 | 主力追蹤（Top 排行 + 走勢圖） | `/branch/{ticker}` | FinMind 法人歷史（替代分點） |
| 6 | AI 白話文判讀 | 在 #4 內 | 後端規則引擎 |

### Why
使用者要在睡覺前把 V2 實驗 tab 完整實作完成，要把所有模擬數據替換為真實 API。FinMind 免費版部分資料（分點、持股分級）需付費，採實用替代方案。

### 待確認/待處理（使用者醒來決定）
- 是否要部署到 Railway（會影響 Benjamin 那邊的紅綠燈）
- 是否要 git commit
- `dashboard_v2_preview.html`（獨立預覽檔）是否要刪除
- ATM 權利金顯示與槓桿效應計算可進一步精緻化
- 加權指數漲跌（spotChange）目前從 MI_INDEX 抓不到，僅有收盤值

---

## 2026-05-09

### 完成項目（錢哨 — 策略管理 UI 設計）
- 設計「錢哨」策略管理 tab 完整 UI，產出預覽檔 `strategy_manager_preview.html`
- 三大模式：策略設定 / 分析結果 / 歷史紀錄
- 策略設定：左側策略清單（新增/刪除/切換）+ 右側六大分類條件設定（基本面、技術面、籌碼面、型態分析、量價分析、策略邏輯解說）
- 新增策略彈窗：勾選模式（手動設定條件）/ 口述模式（自然語言描述 → 系統優化 → 確認建立）
- 分析結果儀表板：技術分析總覽、籌碼分析表、關鍵價位、主力燈號、型態分析（W底/M頭）、技術指標總覽、綜合勝率圓環、操作建議、整體結論
- 歷史紀錄面板：搜尋/篩選/時間範圍、點擊回看、每筆可下載報告
- 報告匯出：HTML 下載 + PDF（開新視窗列印另存）
- 產出後端開發工作項目文件 `錢哨_後端開發項目_V1.md`（12 個 API、9 個引擎模組、預估 6.5hr）
- 建立專案心智圖 `mindmap.html`（互動式 Canvas，涵蓋 7 大分支）
- UI 拆分為雙 Tab：「策略選股」（策略管理 + 執行選股 + 選股結果表格）、「個股分析」（單股分析 + 歷史紀錄）
- 策略選股新增「執行選股」功能：全市場掃描 → 符合條件的股票清單（含代碼/價格/漲跌/均線/KD/MACD/法人/勝率/燈號）+ CSV 匯出 + 排序
- 選股結果每列可一鍵跳轉「個股分析」tab 做深度分析
- Benjamin 串接：projects.json 新增錢哨專案（7 features 進度追蹤）+ stock_server.py 加 `/qianshao` 端點 + Railway 部署完成
- 後端開發文件啟用版本管理（V1/V2/V3 累計，前後端解耦獨立作業）

### 完成項目（錢哨 — 後端開發 Phase A~D 全部完成）
- 新建 `strategy_engine.py`：技術面（MA/KD/MACD/RSI/布林）、籌碼面（TWSE T86 連買連賣 + 主力燈號）、關鍵價位、勝率（條件命中率）、操作建議、型態（W底/M頭/K線訊號：紅三兵/三隻烏鴉/十字線/長下影線/晨星/夜星）、量價（量比/乖離率/量價狀態）
- 新建 `report_generator.py`：渲染獨立 HTML 報告（深色主題 + @media print 白色版），檔名支援中文（RFC 5987）
- 新建 `qianshao.html`：可運作的前端，串接所有後端 API，3 模式（編輯/結果/歷史），條件範本 5 大類 14 個欄位
- `stock_server.py` 新增 9 個端點：`/qianshao.html`、`/strategies` CRUD、`/strategies/analyse`、`/analysis/history` 三件式、`/analysis/history/{id}/report`
- `dashboard.html` 加「錢哨」tab（iframe lazy-load），位置在「策略」與「損益」之間
- 持久化：`strategies.json`、`analysis_history.json`（local JSON）
- 全部 endpoint 用直接呼叫法測試通過（CRUD/analyse/report 端到端）
- 條件 ctx 對應後端欄位：PE/PB/dividend_yield/EPS、k/d/j/rsi/ma_alignment、foreign_buy_days/foreign_sell_days/trust_buy_days/trust_sell_days、w_bottom/m_top、vol_state/volume_ratio/bias_ma20

### 問題與解法（錢哨後端）
- **TWSE T86 一次只能取一天**：calc_chips 內建 module-level cache，5 個交易日依序爬，避免重複請求
- **EPS 沒有獨立 API**：用 PE 反推 (last_price / PE)，季報未實作
- **Content-Disposition 中文檔名 latin-1 編碼錯**：用 `filename="ascii"; filename*=UTF-8''<urlencoded>` 雙格式
- **TestClient 需要 httpx**：改成直接呼叫 endpoint 函式測試，繞過 HTTP

### 待處理（錢哨後端 — 未實作項目）
- 頭肩底/頂、趨勢線偵測（前端條件範本未列入）
- 產業類別篩選（需另外資料源）
- 營收年增率（t05st09 爬蟲未做）
- 口述模式 modal（前端僅 stub，需接 AI API）
- PDF 報告（目前只有 HTML，PDF 走瀏覽器列印）

### 完成項目（錢哨 — V1 規格對齊調整）
- 規格 diff：比對 `錢哨_後端開發項目_V1.md` 與初版差異，產出 10 條主要差異清單
- **路由全改 `/qianshao/` 命名空間**：9 個 CRUD/analyse/history/report 端點重命名
- **新增選股功能**（V1 新增 P5+P6）：
  - `strategy_engine.screen()`：兩段式（fundamental 快篩 → 完整技術分析），無籌碼資料時自動略過該類條件
  - 5 個端點：`POST /qianshao/screen`、`/screen/history`、`GET/DELETE /qianshao/screen/{id}`、`/screen/{id}/export.csv`
  - `screen_history.json` 持久化（30 天自動清理）
  - CSV 匯出含 BOM（Excel 中文相容）
- **欄位 enum 全英文化**（給條件比對用）：
  - 籌碼 `investment_trust` → `trust`
  - 主力燈號 `安全/觀望/危險` → `safe/watch/danger`
  - 型態 `已形成/形成中/未形成` → `formed/forming/none/insufficient`
  - K 訊號改字串陣列（含日期 suffix）
  - `indicators_table` 用 `desc` + `trend: bull/bear/neutral`
  - `indicators` 加 `trend_code`、`kd_cross`、`ma_align_code`、`macd_status`、`change_pct`
  - `analyse()` 結果頂層加 `price`、`change_pct`
- **operator 雙語兼容**：`OPERATORS` 加 `lt/gt/gte/lte/eq/ne` alias
- **report_generator 中文 label 對照表**：英文 enum → 中文顯示（`_LEVEL_LABEL`/`_PATTERN_LABEL`/`_TREND_LABEL`）
- `qianshao.html` 同步 V1 路徑 + 條件範本英文 enum + 顯示層 trend/level/pattern badge 雙語兼容
- `dashboard.html` 錢哨 iframe 路徑改 `/qianshao`（V1 主入口）
- 15 個 V1 路由全數註冊驗證 + CRUD/Analyse/Screen/CSV/Report 端到端測試通過

### 問題與解法（V1 對齊）
- **screen 籌碼條件誤判**：chips_fn=None 時 stats 預設 0，比對 `>= 3` 會 false → 改用 `None`，讓 `_eval_condition` 略過該類條件
- **value_aliases 比對失敗**：原 ctx 用中文（如「多頭排列 (5>10>20)」），V1 用 `bull` → 改成 ctx 直接給英文 enum（中文版保留在 summary 給 UI 顯示）
- **ma_alignment 雙重職責**：summary（中文，UI 顯示）vs ctx（英文 enum，條件比對）→ 命名一樣值不同，靠 dict 隔離不衝突

### 問題與解法（Benjamin 錢哨/戰情儀表板顯示離線）
- 看到使用者截圖戰情儀表板 + 錢哨同時紅燈離線，誤判為 V1 路由改動造成
- 排查：localhost:8000/qianshao=200、/qianshao/strategies=200，新路由完全 OK；但 `100.106.1.28:8000/health` 連不上（exit 28 timeout）
- **根因：Tailscale 沒啟動**（`tailscale status` 回 "Stopped"）→ Benjamin 健檢 URL 是 `http://100.106.1.28:8000/health`（Tailscale IP），打不通
- 解法：`tailscale up` → IP 100.106.1.28 恢復 → 兩個專案同步轉綠
- 教訓：兩個服務同時離線時，先檢查共用網路層（Tailscale/tunnel），再檢查路由

### 待處理（前端串接 — 暫停中）
- **B 階段：strategy_manager_preview.html 串 V1 API**（45 min 預估）
  - 確認該檔案目前是 mock UI（grep 無 fetch 呼叫），表格內容寫死 → 切策略結果都一樣
  - 要做：策略清單 fetch、條件儲存 POST/PUT、執行選股 POST `/qianshao/screen`、執行分析 POST `/qianshao/analyse`、歷史 fetch、報告/CSV 連結
  - **暫停原因**：另一對話正在改 stock_server.py（`/events`、`/macro/dashboard`、`/branch`、擴充 `/fundamental` + `/chip`），跟本次已改的 `_get_fundamental_for_ticker(last_price)`、`/chip` enum 重疊區大，需先協調避免覆蓋衝突
  - **恢復條件**：另一對話完成 stock_server.py 改動後，本次先 `git diff` 確認簽章與 enum 沒被打回去，再動工 B

### 完成項目（video2slides pipeline 建置）
- 建立 video2slides 專案：影片 URL/本地檔 → 自動產出 Google Slides 簡報
- 全模組實作完成：downloader（yt-dlp）、audio_extractor（ffmpeg）、transcriber（faster-whisper）、scene_detector（PySceneDetect）、content_analyzer（Gemini）、slides_generator（Slides+Drive API）、pipeline 主流程、CLI（typer）
- 7 個單元測試全通過（content_analyzer JSON 解析、downloader 本地檔、transcriber SRT 格式）
- 內容分析模組：原規格用 Claude API（付費）→ 改用 Gemini 完全免費
- 轉錄結果與大綱 JSON 快取機制（避免重跑 Whisper 與 Gemini）
- transcriber 預設 use_cache=True（轉錄結果不變、無需重跑）
- 並行執行：Whisper 轉錄與場景偵測同時跑（concurrent.futures）
- Whisper 模型改為 large-v3-turbo（速度快、品質接近）
- 兩支影片實測產出 Slides 成功：
  - YouTube《極簡 10 個習慣》→ 16 張投影片大綱、實際 15 張上傳
  - nschool《台銀金 AU9901 教學》→ 12 張投影片（透過 m3u8 串流）

### 問題與解法
- **Claude Max 不含 API 用量**：Max 訂閱僅給 claude.ai，程式內 API key 另計費 → 改用 Gemini 完全免費
- **Gemini 模型 404 連環錯**：
  - gemini-1.5-flash → 在 v1beta 已下架
  - gemini-1.5-flash-8b → 同樣不存在
  - gemini-2.0-flash → free tier quota: 0（需專案啟用）
  - 最終可用：gemini-2.5-flash-lite（先用 client.models.list() 列出實際可用模型）
- **Generative Language API 未啟用**：到 console 啟用後 quota 解鎖
- **OAuth 同意畫面預設內部模式**：個人 Gmail 被擋（org_internal）→ 改外部 + 加測試使用者
- **新版 Google Auth Platform 沒有下載 JSON 按鈕**：手動依 Client ID/Secret 組 google_oauth.json
- **nschool.tw 不被 yt-dlp 支援**：透過瀏覽器 DevTools 抓 m3u8 簽名 URL，餵給 yt-dlp 解碼
- **google-generativeai 套件已棄用**：改用 `google-genai` 新 SDK（client = genai.Client(api_key)）
- **Pydantic V2 Field(env=...) 棄用警告**：改用 BaseSettings 自動讀取（小寫變數名對應大寫 env）
- **rm 目錄被權限擋**：改在 transcriber/pipeline 加快取邏輯，避免依賴手動清理

### 待處理
- 場景偵測 threshold 調整：教學/訪談型畫面變化少，目前抓不到截圖（SCENE_THRESHOLD=20 仍偏高，可能需要 < 15 並放寬「< 2 秒過渡濾掉」規則）
- 4 小時以上長影片分段處理（spec.md 提到 token 上限）
- README.md 與 Google Cloud 設定教學文件
- 字級時間戳記目前未用於投影片（只用 segment 級）

### 備註
- 專案路徑：`/Users/balianwang/Downloads/balian/video2slides/`
- Gemini API Key（AI Studio 免費層）
- Google OAuth：Client ID 447283172552（My First Project）
- 模型：Gemini 2.5 Flash Lite（內容分析）+ Whisper large-v3-turbo（CPU/int8）
- 執行範例：`.venv/bin/python main.py process "<URL或本地檔>" [--output-only-json] [--use-cache] [--no-screenshots]`
- 已產 Slides：
  - YouTube 範例：https://docs.google.com/presentation/d/1wT52ShxIuerCmWPMx2l_5pRmO-Ii6ykhbMSsclnnwFs/edit
  - nschool 範例：https://docs.google.com/presentation/d/13KGDDoIRFpzTFYaoH12McgyGHofvM5SOJM-5hVcWjwo/edit

### 完成項目（續：Benjamin 整合 + 線上 UI）
- video2slides 改為 FastAPI server（`server.py`）：本機 8001 port，cloudflared quick tunnel 對外
- API：POST /jobs（建任務）、GET /jobs/{id}（查狀態）、GET /jobs（列歷史）、GET /download/{id}/{fmt}（下載）、GET /ui（介面）
- 新增 `ui.html`（Benjamin 風格深色介面）：URL 輸入 + 語言/截圖/類型選項 + 進度卡 + 下載卡，串實際後端
- Benjamin 線上 dashboard 新增「影音轉學習簡報」專案（透過 PUT API → 重啟後永久化需 main.py 修正）
- balian-quant `main.py`：PUT/POST `/api/projects` 補上 `app_url` 與 `restart_url` 持久化（原邏輯只存 name + health_url）
- `railway up` 部署 balian-quant，Benjamin 對 video2slides health-check 通過
- `content_analyzer.analyze()` 加 `forced_type` 參數，UI 上「類型」下拉可強制指定 lecture/meeting/tutorial/interview

### 完成項目（續：檔案結構整理）
- output 目錄改為 job 資料夾管理：
  - `output/jobs/{video_id}/`：影片 + 音訊 + 逐字稿 + outline + screenshots（每影片自成一包）
  - `output/downloads/`：簡報集中下載區，檔名直接用簡報標題（PPTX + PDF）
- pipeline 結尾自動 export PPTX/PDF 到 downloads（不必每次去 Drive 點下載）
- 既有 1080.mp4、MN135KJfSZU.mp4、VUj6zDFs-2Q.mp4 整批 migrate 到新結構

### 完成項目（續：進度追蹤 + 來源 slide）
- 每階段記錄 elapsed：`stages_elapsed`、`current_stage_elapsed`、`total_elapsed`、`stages_estimate`、`estimated_total`
- UI 階段右側 time chip：完成顯示綠色實際時間、進行中顯示藍色累計（每秒滾動）、未開始顯示「約 N 秒」灰色預估
- 進度條下方總計：「已花 X · 剩餘約 Y（預估總計 Z）」、完成後顯示「總計 X / 預估 Y」
- 每份簡報自動加最後一張「來源資訊」slide：原始影片 URL、影片標題、處理時間、video_id（後續可回溯原片）
- 預估參考值（30 分鐘影片基準）：下載 45s / 抽音訊 8s / 轉文字+場景 180s / AI 分析 25s / 生 Slides 20s / 匯出 12s

### 完成項目（續：持久化 + 抗中斷）
- `output/jobs/_meta/{job_id}.json` 持久化每個 job 狀態，server 啟動自動 reload
- 重啟後 running 狀態的 job 自動標 failed（避免假象）；done/failed 完整保留下載功能
- ui.html 用 localStorage 存 v2s_job_id，頁面載入自動 `_restoreJob()` 還原進度/下載/錯誤畫面
- 解決 Benjamin 切換專案 → iframe reload → 進度狀態消失的問題（即使後端還在跑）
- Whisper 從 large-v3 改 large-v3-turbo：速度 3-4x、22 分鐘音訊從 ~13 分鐘 → ~3-4 分鐘（使用者要重點不要逐字稿準確度）

### 問題與解法（補）
- Benjamin renderMain 每次切 project 重新 innerHTML，導致 iframe 銷毀重建 → ui state 改用 localStorage 持久化還原
- `_jobs` dict 只在記憶體，server 重啟時所有 job_id 失效，UI 點下載按鈕 404 → server 寫 `_meta/*.json` + startup reload
- POST /api/projects 沒接 app_url 參數 → 修 main.py 兩個 handler 都接收並寫入
- cloudflared quick tunnel URL 重啟會變，部署 Railway 時 projects.json 寫死無法重複使用 → 暫接受，未來換 named tunnel 才能固定

### 待處理（補）
- cloudflared 換成 named tunnel（網址固定 + 開機自啟）
- video2slides server.py 開機自動啟動（LaunchAgent）
- iframe 持久化：Benjamin dashboard.html 把 iframe 改成預先建立、切換只 toggle 顯示（更乾淨但範圍大，目前 localStorage 已能滿足體驗）
- Google OAuth scope 是 `drive.file`，PPTX/PDF export 若失敗需加 `drive.readonly` 並重新授權

### 備註（補）
- 線上 UI：Benjamin → 影音轉學習簡報 → 操作介面
- 本機 server PID 90713（cloudflare tunnel: engaged-inspection-recruitment-loops.trycloudflare.com）
- 今日另產 Slides：解鎖大腦成癮機制（成癮式學習法）→ 19 張 + 來源 slide
- 後端 endpoints 對應：`/jobs`（建/列）、`/jobs/{id}`、`/download/{id}/{slides|pptx|pdf}`、`/ui`、`/health`

### 完成項目（續：Benjamin 任務完成通知）
- balian-quant `main.py` 的 `update_project` endpoint 加入任務完成 Telegram 通知
  - 比對新舊 features，偵測 `done: false → true` 的項目
  - 自動發送：`*任務完成* {專案名稱}\n\n{feature 名稱}`
  - 透過既有 `notifier.py` 的 `_send()` 發送，不需額外設定
- `railway up` 部署上線，health check 通過

### 完成項目（續：Benjamin 架構盤點）
- 確認 Benjamin 完整架構並寫入 memory：
  - Dashboard 主系統：`projects/balian-quant/`，Railway `balian-quant`
  - 提醒機器人 (gent)：`projects/reminder-bot/` + GitHub `balianqbar-eng/gent`，Railway `charming-fulfillment`
- Railway 線上 dashboard（`/dashboard`）專案資料為空（projects.json 未同步），截圖中的資料來自本地開發時寫入的 projects.json

---

## 2026-05-08

### 完成項目（續：Mac 儲存空間清理）
- 確認 Mac 各儲存類別意義：開發者、其他使用者與共享項目、系統資料、macOS
- 確認無本機 Time Machine 快照（tmutil listlocalsnapshots 為空）
- 查出前幾大快取：Google 930MB、Claude ShipIt 697MB、Homebrew 213MB、node-gyp 62MB、pip 53MB
- 清除 Claude ShipIt / node-gyp / pip 快取 + brew cleanup（釋放約 1.2 GB）
- 確認清除不影響 Benjamin 正在執行的程式

### 完成項目（續：reminder-bot + Telegram 通報）
- **reminder-bot** 建立並部署至 Railway（`charming-fulfillment` 專案）
  - Telegram bot：`@balian_reminder_bot`，指令：/add /list /done /today /status
  - PostgreSQL 持久化待辦事項，每天 08:00 Asia/Taipei 自動推送日報
  - webhook 模式（`https://reminder-bot-production-6453.up.railway.app`）
- **balian-quant Telegram 即時通報整合**（`notifier.py`）
  - `/analyse/{symbol}` 觸發 → 個股回測結果即時推 Telegram
  - `/screen` 觸發 → 選股結果即時推 Telegram
  - `/test-notify` 端點：測試通報連線
- reminder-bot 加入 `/status` 指令：查詢 Balian Quant API 與 XQ Bridge 連線狀態
- 修復 3 個部署 bug：Python 3.13 slots 不相容、APScheduler event loop 順序、Railway 多 instance polling 衝突

### 完成項目（續：新對話修 bug）
- `notifier.py` — `result['total_return']` 改為 `s.get('total_return_pct', 0)`，配合新 backtest 結構（`{"summary":{...}, "trades":[...]}`）
- `main.py /big-player` — 改用 `pandas.to_json(orient="records")` 處理 NaN→null，解決 FastAPI 無法序列化 numpy NaN（ValueError: Out of range float values are not JSON compliant）
- 全端點驗證通過：/health /analyse /institutional /margin /chips /big-player /test-notify 均回 200
- /big-player/2330 警訊正常：4/24、4/27（散戶異常）、4/28（大戶異常）

### 完成項目
- XQ COM 介面完整診斷：CLSID 存在但無 InprocServer32/LocalServer32，確認為 Excel 專用，Python 無法外部使用
- XQ port 8088 確認為 CEF 內部 UI server，非資料 API；DDE 需 MFC message loop，不適用 server 環境
- 三資料來源架構（balian-quant）：永豐（Shioaji）/ XQ bridge / FinMind
  - adapters/base.py：DataSource 抽象介面
  - adapters/finmind.py：kbar + 三大法人 + 融資融券
  - adapters/yuanta.py：永豐 Shioaji adapter（kbar + realtime snapshot）
  - xq_client.py 重構：kbar 日K→FinMind，realtime→永豐，XQ bridge 備援
- FinMind 融資融券端點：/margin/{symbol}
- 三大法人端點：/institutional/{symbol}
- 籌碼分數端點：/chips/{symbol}（chip_score，加權三大法人＋融資）
- 大戶/散戶買賣力：/big-player/{symbol}
  - big_player_power()：外資＋投信淨買（大戶）、融資淨買（散戶）
  - z-score 警訊（偏離 20 日均值 2 個標準差觸發）
  - 322 天中 35 筆警訊（合理比例）
- 漲停二次鎖偵測：monitors/limit_up.py
  - 狀態機：idle → locked → unlocked → second_lock（觸發 Telegram）
- 大戶買賣力盤中監控：monitors/big_player.py
  - Shioaji tick 訂閱，單筆 ≥10 張=大軍、≥20 張=特大軍、≤5 張=散戶
  - 滾動窗口淨量超過閾值 → Telegram 警訊
  - 端點：POST /monitor/big-player/start、GET /monitor/big-player/status
- 回測強化（backtest.py）：完整交易明細（進出日期/價格/張數/手續費/淨損益/報酬%/持有天數/勝敗）＋分析報告（勝率、平均獲利虧損、profit factor、最大連勝連敗、max drawdown）
- /analyse 加 start_date / end_date / capital 參數（介面可設定回測區間）
- 永豐 Shioaji 連線成功（simulation=true，帳號：王彥皓 N124835954，broker 9A9W）
  - SINOPAC_API_KEY / SINOPAC_SECRET_KEY / SINOPAC_SIMULATION 已設入 Railway
- Benjamin 儀表板隱藏「介面網址」欄位（display:none，資料保留）
- XQ 自選連線錯誤訊息改善：顯示具體 HTTP 狀態碼＋伺服器 detail（紅色）
- Win11 StartXQ Task Scheduler 建立（schtasks /TN StartXQ，balianwang session 1）
- stock_server.py 加 POST /start-xq 端點（自動偵測 XQ 是否已執行）
- dashboard.html 加「啟動 XQ」按鈕（點擊後 10 秒自動重整自選清單）

### 問題與解法
- FinMind TaiwanStockTradingDailyReport 不存在（422）：分點資料為付費功能，改用 InstitutionalInvestorsBuySell
- big_player_power 警訊閾值 pct_change 在接近 0 時爆（inf）→ 改用 z-score
- Shioaji production key 錯誤（"Token doesn't have production permission"）→ simulation=true 可用，snapshot 為真實市場資料

### 待處理
- screener.py universe() 仍依賴 XQ bridge（COM 壞），需改用 TWSE API 取全市場清單
- 永豐 kbar 實際測試（simulation 模式下 kbar 是否回傳真實歷史資料）
- 券商分點籌碼（FinMind 付費）或 TWSE 爬蟲取得
- 申請永豐正式 API 權限（目前 simulation=true，下單走模擬）

### 備註
- balian-quant Railway URL：https://balian-quant-production.up.railway.app
- FinMind 免費：600 req/hr（需 token，環境變數 FINMIND_TOKEN）
- 分點資料（券商籌碼）FinMind 付費，暫不實作
- Win11 XQ 安裝路徑：C:\SysJust\XQLite\daqxqlite.exe
- Win11 StartXQ Task Scheduler 已建立，可從 Mac 觸發

---

## 2026-05-06

### 完成項目
- Benjamin 開發整合儀表板建立（Railway 部署）：專案清單側欄、進度條、功能清單、health check、操作介面 Tab（iframe）
- 儀表板 Contract-First + Adapter Layer 架構：新增 schemas.py、xq_client.py，重構 main.py / screener.py
- 儀表板 API：/dashboard、/api/projects CRUD、/api/projects/{id}/health 五個端點
- Health check 改為瀏覽器端 fetch（解決 Railway 無法 ping Tailscale 私有 IP 問題）
- xq_bridge/server.py 加入 CORSMiddleware
- XQ 橋接服務修復：xq_bridge.py lazy init（不在啟動時 Dispatch COM）+ pythoncom.CoInitialize()
- 正確 COM 類別名稱確認：XQAddin.Addin（非 XQData.XQDataManager）
- stock_server.py 加入 /restart-xq 端點（prlctl exec Win11 kill port 8000 PID + 重啟 server.py）
- Win11 xq_server.py 手動啟動成功（port 8000 online，health 回 200）
- XQ 自選商品組合清單串接：xq_server.py 新增 `/watchlists`、`/watchlist`、`POST /watchlist` 端點
- 從 XQLite 備份檔（SymbolList XML）解析 42 個商品組合寫入 portfolios.json（含成分股）
- 商品組合帶股票名稱：xq_server.py 新增 `_get_stock_names()` 快取 TWSE 名稱，/watchlist 回傳 `{ticker, name}`
- 修復 XQ server `/health` 掛起問題：移除 COM 執行緒阻塞，health 改為即時回傳
- 用 `prlctl exec` 從 Mac 直接啟動 Win11 的 xq_server.py（conda run）
- 建立 `start_xq_server.bat`，放進 Win11 Startup 資料夾可開機自動啟動
- Telegram 通知整合：stock_server.py 加入 `_tg()` 函數，訊號產生與下單成功時推播
- 從 Railway CLI 取得 TELEGRAM_TOKEN / CHAT_ID，填入 `.env`，測試發送成功
- balian-quant 重構（XQClient 封裝）+ `railway up` 部署上線

### 問題與解法
- Railway health check ping 不到 Tailscale IP → 改成瀏覽器端 JS fetch 直接 ping
- prlctl exec 在 session 0 無法存取 session 1 的 COM 物件 → xq_bridge 改 lazy init，server 可在 session 0 啟動
- XQData.XQDataManager COM 類別不存在 → 正確類別為 XQAddin.Addin，但 COM 呼叫需 XQ 開啟且 CoInitialize
- projects.json trailing comma → JSON 不允許，修正後重新部署
- prlctl exec 傳遞 Python 程式碼引號被吃掉 → 改用 base64 編碼傳遞
- XQ `/health` 回 500：COM 執行緒在非互動模式阻塞 → 移除 COM 健康檢查，直接回傳 ok
- prlctl exec 語法：指令不能加引號包住 VM 名稱外的部分，用 `prlctl exec UUID cmd /c "..."` 語法
- portfolios.json 初始為空 → 從 XQLite Backup SymbolList XML 解析 big5 編碼取得完整清單

### 待處理
- XQ COM 實際可用介面待確認（XQAddin.Addin 方法名稱需在 session 1 測試 /methods 端點）
- XQ 橋接服務開機自動啟動（目前需手動執行或觸發重新連線按鈕）
- XQ 商品組合：部分 OTC 小股名稱顯示為代碼（TWSE API 未收錄）

### 備註
- Win11 Tailscale IP: 100.107.36.65，Mac: 100.106.1.28
- Telegram CHAT_ID: 8375864518（reminder-bot 同一頻道）
- Win11 Python: C:\Users\balianwang\miniconda3\python.exe
- XQ bridge 啟動指令：prlctl exec dcf8c674-794d-426f-95d9-8d397f211a64 cmd /c "cd /d C:\Balian\xq_bridge && start /B C:\Users\balianwang\miniconda3\python.exe server.py"

---

## 2026-05-05

### 完成項目
- 建立戰情儀表板開盤前置清單（開盤前置清單.md）
- 新增股票查詢後顯示中文名稱（TWSE API 查詢）
- 修復大查詢失敗問題（fmt() null-safe + TypeError 分類處理）
- 建立母/子頁面多股票監控架構（1 母 + 10 子）
- 手機版 RWD 響應式排版（Tailwind sm: breakpoints）
- 整合 XQ 自選清單前端 UI（群組下拉選單）
- XQ bridge 後端新增 /watchlists、/watchlist 端點
- 安裝 Tailscale 解決外網連線問題（Mac 100.106.1.28、Win11 100.107.36.65）
- macOS LaunchAgent 自動啟動 stock_server.py
- Win11 bat 檔自動啟動 XQ bridge
- 程式碼備份至 GitHub（balianqbar-eng/trade）
- 查詢歷史記錄：登入後清空、最多保留 10 筆

### 問題與解法
- SSL 驗證失敗：TWSE API 加 verify=False
- 查詢錯誤訊息誤導：TypeError 獨立捕捉，區分「資料顯示錯誤」vs「連線失敗」
- 外網連線不穩定：改用 Tailscale 固定 IP，取代 ngrok

### 待處理
- XQ bridge Win11 pandas 安裝確認與測試
- XQ GetPortfolioNameList COM 方法名稱驗證

### 備註

---

## 2026-05-14（美甲美睫店 SOP 設計 + 內容生產 3 版示意）

### 完成項目
- 用 harness-coach 教練模式陪使用者規劃美甲美睫店日常 SOP
- Phase 1：產出店面日常「角色 × 時段 × 環節」總覽架構表（5 區塊：開店前 / 客戶服務 / 客戶間空檔 / 閉店後 / 跨日經營）+ 獲客漏斗對應表
- Phase 2A：針對使用者三大痛點（內容生產耗時 / 數據分析無時間 / 客群不足）給出策略架構與 30 天執行優先序
- 校準店家畫像後（住商混合 / 客單 2500-4000 / 36-45 熟齡 / 月預算 1-3K / 主力美睫附帶美甲），重排平台優先級（FB > Google Map > LINE OA > IG > 小紅書，砍 Threads/TikTok）
- Phase 2B：內容生產 SOP（拍照 30 秒 SOP / 限動 4 行模板 / 文案 5 大公式 / 內容週節奏表 / 一日生產時間表）
- 內容自動化階段 1：產出 3 版設計示意稿 HTML（A 質感極簡 / B 前後對比 / C 情境療癒），含 IG 貼文 1080×1350 + 限動 1080×1920 + 短/長文案，使用者選定 A 版
- 配色採用米白 + 燕麥 + 奧茉綠 + 金，呼應 36-45 熟齡女性療癒高級感定位
- 寫入新 memory：feedback_ask_question_tool.md（問問題優先用 AskUserQuestion 工具，不在對話列 markdown 問題清單讓使用者複製回填）

### 問題與解法
- 使用者反映「列 markdown 問題清單要複製刪填很麻煩」→ 改用 AskUserQuestion 工具呈現選項介面 → 寫進 memory 永久規則
- 使用者說「產出」但未附素材 → 擋下，避免憑空生成虛擬眼睛+假店名導致無法驗證真實品質 → 改用 SVG 佔位符走通流程

### 待處理
- 等使用者提供真實素材（客戶眼睛照 / 店名 / IG handle / 款式）→ 替換 SVG 佔位符 → 輸出可發布 PNG 檔
- 階段 2：建半自動上稿介面（上傳照片 → 自動套版 → 一鍵下載四平台尺寸）
- 階段 3：串接 Meta / LINE / Google 商家 API，自動排程發布
- 後續尚未做的模組：LINE OA 衝好友 + 分眾 + 推播、Google Map 評論衝刺、FB 在地社團經營、異業合作

### 備註
- 工作檔案：nail_eye_content_pilot/preview.html（已開瀏覽器預覽）
- 使用者預計優化到 8 成後，由 Obsidian 生成新 Vault 收錄完整 SOP
- 階段 1 done-condition：使用者看到產出說「OK 可以發」即過關（目前卡在等真實素材）

---

## 2026-05-14（遠端工作環境規劃：舊 iMac → Mac mini）

### 完成項目
- 使用者場景釐清：外出想用舊 iMac Air（High Sierra 10.13）連回家裡 Mac mini 監控 Claude Code、遠端桌面、下指令
- 提出兩條技術路線並比較缺點：
  - 路線 A：Tailscale 舊版 GUI（1.40 系列，最後支援 High Sierra 的版本）
  - 路線 B：ZeroTier（相容性好，但無 MagicDNS / 速度較慢 / iOS app 較陽春 / 設定 UI 工程師導向 / 社群資源少）
- 使用者決定走路線 A
- 給出舊 iMac 端 Tailscale 1.40 完整安裝 SOP（下載點 / Gatekeeper 處理 / 登入流程 / ping 100.x.x.x 驗證）
- 卡關處理清單：版本不相容退回 1.38/1.36/1.34；防火牆擋連入需手動允許

### 問題與解法
- High Sierra 是整套方案最大瓶頸 → Tailscale 1.50+ 要求 Big Sur → 改抓 1.40 舊版 pkg 避開
- Homebrew 在 macOS 11 以下已停止支援 → 不走 `brew install tailscale` 路線

### 待處理
- 使用者尚未回報舊 iMac 端 Tailscale 安裝結果（卡在 Step 1-4 哪一步未知）
- Mac mini 端 Tailscale 安裝（等舊 iMac 搞定再做，預估 5 分鐘）
- 螢幕共享 / SSH / ttyd 監控三層工具設定（Tailscale 通了才有意義）

### 備註
- 整套架構：舊 iMac（外出端）→ Tailscale VPN → Mac mini（家裡），Mac mini 上跑 ttyd + tmux 讓網頁監控 Claude Code 進度
- 同帳號登入是關鍵：Tailscale 雙端必須登同一個 Google/Apple ID 才能互通
- 工作為純諮詢規劃，無程式碼異動 → 無需 git commit / Benjamin API / smoke_test

---

## 2026-05-14（遠端工作環境通電：ZeroTier + SSH 連線層完成）

### 完成項目
- 釐清需求：把 Mac mini 當伺服器，雙人（Balian + 老婆）從外面 SSH 進來操作 Claude Code，24/7 開機
- 路線選擇：放棄 Tailscale（High Sierra 不支援當前版本）→ 改走 ZeroTier
- 兩台設備通電完成：
  - **Mac mini**（M4, 16GB, macOS 26.3.1）：ZeroTier 1.16.1, Node ID `360f38904d`, ZT IP `10.198.250.154`
  - **MacBook Air**（2015 13", 4GB, High Sierra 10.13）：ZeroTier 1.16.1, Node ID `3bfe4a1dcd`, ZT IP `10.198.250.227`
  - Network ID：`88c5b1f33993b3fc`（名稱 my-first-network, Private）
- SSH server 在 Mac mini 啟動成功（GUI 路徑：系統設定 → 一般 → 共享 → 遠端登入）
- 端對端驗證通過：MBA → ZT overlay → Mac mini，ping 0% loss / 平均 5.6ms / SSH 進入並 hostname 確認

### 問題與解法
- macOS 26 TCC 擋 root 程序讀 ~/Downloads → `cp` 到 `/tmp` 再 `installer -pkg`
- macOS 26 `systemsetup -setremotelogin` 要 Full Disk Access → 改 GUI 開啟（系統設定 → 一般 → 共享）
- `zerotier-cli` 預設只 root 讀 authtoken → `cp /Library/Application Support/ZeroTier/One/authtoken.secret` 到 user home 並 chown
- MBA 第一次裝 ZeroTier 後 `ping: Cannot allocate memory`，所有 ZT 流量過不去 → 重開機後 kernel mbuf 池重置，立刻通
- ZeroTier 1.16 在 macOS 改用 feth driver（不是傳統 zt 介面），listnetworks 顯示 `feth3466` 而非 `zt0`

### 兩次 R002 衍生失敗（已 ratchet 進 R003）
- 我說「Tailscale 1.40 支援 High Sierra」未驗證來源，實際 pkg URL 全部 404
- 我說「`!` prefix 給 sudo 拿到 tty」未驗證，實際撞 `terminal is required`
- 寫進 failures.md R003：URL 推薦前必須 probe / 跨工具執行模型不靠記憶 / 舊軟體支援度不靠記憶 / 同 session 重複失敗模式要立刻 ratchet

### 待處理
- SSH key 免密碼登入（避免每次連都打密碼）
- ~/.ssh/config 別名（`ssh macmini` 取代 `ssh balianwang@10.198.250.154`）
- tmux 自動啟動 + Claude Code 持續執行
- 不睡眠 + 斷電復原設定（pmset / 系統設定）
- 雙人帳號分流策略決定（共用 user / 各自獨立 user / 同 user 不同 tmux session）
- **外出實測**：MBA 在 iPhone 熱點 / 咖啡店 WiFi 下能否仍透過 `10.198.250.154` 連回

### 備註
- ZeroTier 後台網址：https://my.zerotier.com/network/88c5b1f33993b3fc
- ZeroTier 帳號管理：https://account.zerotier.com（跟 my.zerotier.com 拆開）
- 整體 done-condition 達成：MBA 在同 LAN 下能 SSH 進 Mac mini
- 工作為基礎建設設定，無程式碼異動 → 無需 git commit / Benjamin API / smoke_test

---

## 2026-05-14（遠端工作環境延伸：ttyd / VNC / 中文渲染試水 + 結論）

### 完成項目（保留下來的東西）
- **VNC / 螢幕共享**：Mac mini 端「螢幕共享」開啟，port 5900 listening。但 MBA High Sierra 端 client 跟 macOS 26 server 跨 9 年版本差，鍵盤事件在 cmux 視窗內被丟掉（其他 app 可打字），第三方 VNC 也救不了
- **ttyd 網頁終端機**：Mac mini 端 `brew install ttyd` + `brew install tmux`，寫 LaunchAgent `~/Library/LaunchAgents/com.balian.ttyd.plist`，KeepAlive + RunAtLoad 都開，啟動 `tmux new-session -A -s cc` 並 expose 為 `http://10.198.250.154:7681`
- **ttyd 字型 fallback**：Menlo + PingFang TC + Microsoft JhengHei + Heiti TC + Apple LiGothic + Consolas + monospace
- **pmset 24/7 設定**：sleep=0、autorestart=1、disksleep=0、womp=1（Wake on LAN）

### 失敗與已知限制
- **VNC 路線死於版本差**：High Sierra (2017) 跟 macOS 26 (2026) 的螢幕共享協議差太多。cmux GUI 視窗收不到 VNC 傳來的鍵盤事件，這是 Apple 沒留意舊版相容
- **ttyd 渲染破碎**：Claude Code TUI 用了大量 box drawing (U+2500-257F)、CJK、emoji、ASCII art 拼接 logo。xterm.js + 字型 fallback 處理 East Asian Width 永遠會破，這是 web terminal 通病而非設定問題。即使加 PingFang TC 字型 fallback，框線、底線、特殊字元仍然破碎
- **中文 IME 在 web terminal 是次等公民**：composition events 處理不完整，建議用「外部編輯器寫好 → 複製貼上」最穩
- **MBA High Sierra 整體不適合當這套架構的終端**

### 教練自身踩坑（同 session 內第三次 R002 模式）
- 字型問題改了一輪 fallback 仍沒救活渲染，我差點要繼續加 theme JSON、追加 xterm options 想救前一個猜測
- 教練擋下：R003 規則「重複的失敗模式要追加規則，不准把它當『下次注意』」開始發揮作用，我自己叫停讓使用者決定方向

### 結論
- **整套架構保留**：ZeroTier + SSH + ttyd LaunchAgent 全部留著不拆。MBA 這個終端先擱置
- **未來換設備時可即插即用**：
  - iPad / iPhone：裝 Tailscale iOS app 或 ZeroTier iOS app + Termius / Blink SSH client 或瀏覽器連 ttyd
  - 新買 Mac：螢幕共享 + Tailscale 最新版 + SSH 一條龍
  - Windows 筆電：ZeroTier Windows client + PuTTY / WSL ssh + Edge/Chrome 連 ttyd

### 服務清單（24/7 跑著）
- ZeroTier daemon（Mac mini Node `360f38904d` @ `10.198.250.154`）
- sshd（macOS 內建，port 22）
- screensharingd（port 5900，VNC）
- ttyd LaunchAgent `com.balian.ttyd`（port 7681，tmux session `cc`）
- pmset 24/7 + Wake on LAN

### 備註
- 工作日誌中 R003 三條規則發揮效用——這次我自己跑出「鑽下去要被擋」的判斷力，沒等使用者開口
- 整體 done-condition：✓ ZeroTier 通電 / ✓ SSH 通 / ✓ ttyd 通 / ✗ MBA cmux 視窗鍵盤事件 / ✗ ttyd 對 cmux 的渲染品質
- 工作為基礎建設設定，無程式碼異動 → 無需 git commit / Benjamin API / smoke_test

---

## 範本（複製貼上用）

```
## YYYY-MM-DD

### 完成項目
-

### 問題與解法
-

### 待處理
-

### 備註

```
